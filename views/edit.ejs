<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8' />
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css?family=Fira+Sans+Extra+Condensed" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>

<h1><%= title %></h1>
<p>Welcome to <%= title %><br>.</p>

<pre><code>
        <%= JSON.stringify(diaryEntry, null, 4) %>
    </code></pre>

<form action="<%= '/' + diaryEntry._id.toString() %>" method="post">

    <fieldset>
        <label for="title">Title: </label>
        <input id="title" name="title" type="text" value="<%= diaryEntry.title %>">
    </fieldset>
    <fieldset>
        <label for="body"></label>Body:
        <textarea name="body" id="" cols="30" rows="10"><%= diaryEntry.body %></textarea>
    </fieldset>

    <input type="submit" id="submit" name="submit" class="submit" value="Save!">
</form>
</body>
</html>
