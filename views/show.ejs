<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8' />
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css?family=Fira+Sans+Extra+Condensed" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>

<h1><%= title %></h1>
<p>Here's your entry...</p>

<form action='/' method='put' class='display'>

  <p>
    Last updated: <%= diaryEntry.getDate(diaryEntry.lastUpdated).format('MM-DD-YYYY') %> at <%= diaryEntry.getDate(diaryEntry.lastUpdated).format('HH:mm') %>

    <a href='/'><button class='button floatRight' method='delete'>Delete</button></a>
  </p>

  <textarea name="body" rows='8' cols='100' placeholder='<%=diaryEntry.body%>'></textarea>

  <!-- <a href='/'><button class='button floatRight'>Save</button></a> -->
  <input class="button floatRight" type="submit" name="save" value="Save">
  <br />

</form>

<!-- <p class='display'>



  </form>

  <%= diaryEntry.body %>
  <a href='/'><button class='button floatRight'>Delete</button></a>
  <br />
  <br />

  Tags:
  <% diaryEntry.tags.forEach(function(element) { %>
    <%= element %>
  <% }) %>
  <br />
  <br />

  Created: <%= diaryEntry.getDate(diaryEntry.created).format('MM-DD-YYYY') %> at <%= diaryEntry.getDate(diaryEntry.created).format('HH:mm') %>

</p> -->

<a href='/'><button class='button'>ShrinkBot this entry!</button></a>
<!-- <pre><code>
        <%= JSON.stringify(diaryEntry, null, 4) %>
    </code></pre> -->
</body>
</html>
